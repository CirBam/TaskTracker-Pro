# TaskTracker-Pro
TaskTracker Pro is a task and assignment tracker system designed to be a new, self-contained web-based application.

Uses the following languages and libraries:
Python 3.13.5
  (SQLAlchemy, 
  date,
  flask)

To run, launch the app.py file in the backend folder and then open the login.html file using a search browser.
